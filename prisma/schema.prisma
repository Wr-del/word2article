generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Article {
  id          Int      @id @default(autoincrement())
  title       String
  content     String
  translation String?
  difficulty  String   @default("cet4")
  style       String   @default("story")
  createdAt   DateTime @default(now())
  words       Word[]
}

model Word {
  id         Int      @id @default(autoincrement())
  word       String
  phonetic   String?
  definition String?
  chinese    String?
  example    String?
  articleId  Int
  article    Article  @relation(fields: [articleId], references: [id], onDelete: Cascade)
}

model Favorite {
  id         Int      @id @default(autoincrement())
  word       String   @unique
  phonetic   String?
  definition String?
  chinese    String?
  createdAt  DateTime @default(now())
}
